diff --git a/node_modules/expo/src/async-require/messageSocket.native.ts b/node_modules/expo/src/async-require/messageSocket.native.ts
index 55b3c43..67858cc 100644
--- a/node_modules/expo/src/async-require/messageSocket.native.ts
+++ b/node_modules/expo/src/async-require/messageSocket.native.ts
@@ -6,35 +6,31 @@ declare namespace globalThis {
 }
 
 if (__DEV__) {
-  const socket: WebSocket = (() => {
-    const devServer = getDevServer();
-    if (!devServer.bundleLoadedFromServer) {
-      throw new Error('Cannot create devtools websocket connections in embedded environments.');
-    }
-
+  const devServer = getDevServer();
+  if (devServer.bundleLoadedFromServer) {
     const devServerUrl = new URL(devServer.url);
     const serverScheme = devServerUrl.protocol === 'https:' ? 'wss' : 'ws';
-    return new WebSocket(`${serverScheme}://${devServerUrl.host}/message`);
-  })();
+    const socket = new WebSocket(`${serverScheme}://${devServerUrl.host}/message`);
 
-  socket.onmessage = (message: { data: any }) => {
-    const data = JSON.parse(String(message.data));
-    switch (data.method) {
-      case 'sendDevCommand':
-        switch (data.params.name) {
-          case 'rsc-reload':
-            if (data.params.platform && data.params.platform !== process.env.EXPO_OS) {
-              return;
-            }
-            if (!globalThis.__EXPO_RSC_RELOAD_LISTENERS__) {
-              // server function-only mode
-            } else {
-              globalThis.__EXPO_RSC_RELOAD_LISTENERS__?.forEach((l) => l());
-            }
-            break;
-        }
-        break;
-      // NOTE: All other cases are handled in the native runtime.
-    }
-  };
+    socket.onmessage = (message: { data: any }) => {
+      const data = JSON.parse(String(message.data));
+      switch (data.method) {
+        case 'sendDevCommand':
+          switch (data.params.name) {
+            case 'rsc-reload':
+              if (data.params.platform && data.params.platform !== process.env.EXPO_OS) {
+                return;
+              }
+              if (!globalThis.__EXPO_RSC_RELOAD_LISTENERS__) {
+                // server function-only mode
+              } else {
+                globalThis.__EXPO_RSC_RELOAD_LISTENERS__?.forEach((l) => l());
+              }
+              break;
+          }
+          break;
+        // NOTE: All other cases are handled in the native runtime.
+      }
+    };
+  }
 }
